import {Validators} from '@angular/forms';

export interface Property {
  title: string;
  category: string;
  surface: string;
  rooms: string;
  description?: string;
  price: string;
  sold: string;

}
